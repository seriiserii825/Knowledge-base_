### install with yay

```bash
yay -S beekeeper-studio-bin
```

connect on my server

.env

```
DB_USERNAME=username
DB_PASSWORD=userpass
DB_NAME=teashop
DB_PORT=dbport
```

in beekeeper studio instead of localhost use the server ip addresses3

```bash
curl -4 ifconfig.me
```
