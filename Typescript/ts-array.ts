const first: string[] = ['a', 'b', 'c'];
const second: Array<string> = ['a', 'b', 'c'];
const third: (string | number)[] = ['a', 'b', 'c', 1];
const fourth: Array<string | number> = ['a', 'b', 'c', 1];
