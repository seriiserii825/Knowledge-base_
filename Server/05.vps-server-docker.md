### 2. Установите необходимые пакеты


```bash
sudo apt install -y apt-transport-https ca-certificates curl software-properties-common
```
### 3. Добавьте GPG ключ Docker


```bash
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
```
### 4. Добавьте репозиторий Docker


```bash
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```
### 5. Установите Docker


```bash
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```
### 6. Проверьте установку


```bash
sudo docker --version
sudo docker compose version
```
### 7. Добавьте пользователя node в группу docker


```bash
sudo usermod -aG docker node
```
### 8. Применить изменения группы


# Выйдите и зайдите заново
```bash
exit
```

# Подключитесь снова
```bash
ssh node@72.62.93.126
```

# Проверьте, что docker работает без sudo
```bash
docker --version
docker ps
```
9. Включите автозапуск Docker

```bash
sudo systemctl enable docker
sudo systemctl start docker
```
