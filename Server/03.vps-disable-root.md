### 5️⃣ Отключаем root-доступ и пароль по SSH

Открываем конфиг:

```bash
sudo vi /etc/ssh/sshd_config
```

Меняем / проверяем:

```bash
PermitRootLogin no
PasswordAuthentication yes
PubkeyAuthentication yes
```

👉 ВАЖНО: убедись, что вход по ключу под node работает до перезапуска SSH.

Перезапуск:

```bash
sudo systemctl restart ssh
```

Теперь:

❌ root не может зайти
❌ вход по паролю 
✅ только ключи
