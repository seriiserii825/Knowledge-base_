
### после запуска если добавили .env нужно перезапустить контейнеры
```bash
docker compose down -v
docker compose up -d
```

### посмотреть с какими настройками залогинился
```bash
docker exec -it <pgadmin_container_name> env | grep PGADMIN
```
