### Удаление строк, начинающихся с HTML-комментария

```vim
:g/^\s*<!--/d
```

```vim
^ — начало строки
\s* — ноль или больше пробельных символов (пробелы, табы)
```

<!-- — начало HTML-комментария
d — delete (удалить такие строки)
