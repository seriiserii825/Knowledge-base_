# Install

```bash
sudo pacman -S zsh
```

## Set as default shell

```bash
chsh -s $(which zsh)
```

### Restart your terminal or log out and back in for the changes to take effect.

```bash
echo $SHELL
```
