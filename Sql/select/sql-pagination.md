# SQL Pagination - Руководство

## Основные понятия

| Термин                | Описание                   | Пример     |
| --------------------- | -------------------------- | ---------- |
| **page**              | Номер страницы             | 1, 2, 3    |
| **limit** (page_size) | Записей на странице        | 10, 20, 50 |
| **offset**            | Сколько записей пропустить | 0, 10, 20  |
| **total**             | Всего записей в БД         | 247        |

---

## 🔢 Формулы

### Главная формула

```
OFFSET = (page - 1) × limit
```

### Примеры расчета

| Страница | Limit | OFFSET | Формула         |
| -------- | ----- | ------ | --------------- |
| 1        | 10    | 0      | (1-1) × 10 = 0  |
| 2        | 10    | 10     | (2-1) × 10 = 10 |
| 3        | 10    | 20     | (3-1) × 10 = 20 |
| 5        | 20    | 80     | (5-1) × 20 = 80 |

### Общее количество страниц

```
total_pages = CEIL(total / limit)
```

**Примеры:**

- 247 записей, limit=10 → `CEIL(247/10) = 25` страниц
- 47 записей, limit=10 → `CEIL(47/10) = 5` страниц

---

## 📖 Синтаксис SQL

### Базовая структура

```sql
SELECT columns
FROM table
WHERE conditions
ORDER BY column    -- ⚠️ ОБЯЗАТЕЛЬНО для стабильных результатов
LIMIT количество   -- сколько записей вернуть
OFFSET сколько_пропустить;  -- сколько пропустить
```

### Примеры

```sql
-- Первая страница (первые 10 записей)
SELECT * FROM products
ORDER BY id
LIMIT 10 OFFSET 0;

-- Вторая страница (записи 11-20)
SELECT * FROM products
ORDER BY id
LIMIT 10 OFFSET 10;

-- Третья страница (записи 21-30)
SELECT * FROM products
ORDER BY id
LIMIT 10 OFFSET 20;
```
