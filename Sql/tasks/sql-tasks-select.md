# SQL Практика: ORDER BY, LIMIT, OFFSET, Pagination

### Задача 5

Показать заказы, отсортированные сначала по статусу (PAID, потом PENDING), затем по дате создания (новые первыми).

```sql
select * from orders ORDER BY status = 'PAID' DESC, status = 'PENDING' DESC, "createdAt" DESC;
```

### Задача 6

Вывести отзывы, отсортированные по рейтингу (от высокого к низкому), а при одинаковом рейтинге - по дате (новые первыми).

```sql
select * from reviews ORDER BY rating DESC, "createdAt" DESC;
```

## 🔢 Блок 2: LIMIT (Задачи 9-15)

### Задача 9

Показать 5 самых дорогих продуктов.

```sql
SELECT id,title,price, "createdAt" FROM products ORDER BY price DESC LIMIT 5;
```

## ⏭️ Блок 3: OFFSET (Задачи 16-20)

### Задача 19

Пропустить 5 самых дорогих продуктов и показать следующие 3.

```sql
SELECT id,title,price, "createdAt" FROM products ORDER BY price DESC LIMIT 3 OFFSET 5;
```

## 📄 Блок 4: Pagination (Задачи 21-30)

### Задача 21

Получить **страницу 3** продуктов, если на странице показывается **12 продуктов**. Сортировка по дате создания (новые первыми).

```sql
SELECT id,title,price, "createdAt" FROM products ORDER BY "createdAt" DESC LIMIT 4 OFFSET 12;
```

## 🎯 Напоминания

**Формула для OFFSET:**

```
OFFSET = (page - 1) × limit
```

**Структура SQL запроса:**

```sql
SELECT columns
FROM table
WHERE conditions
ORDER BY column
LIMIT количество OFFSET сколько_пропустить;
```

**Таблицы Teashop:**

- `users` - пользователи
- `stores` - магазины
- `products` - продукты
- `categories` - категории
- `colors` - цвета
- `orders` - заказы
- `order_items` - товары в заказах
- `reviews` - отзывы

---

## 📝 Полезные колонки для сортировки

- `createdAt` - дата создания
- `updatedAt` - дата обновления
- `price` - цена
- `title` / `name` - название
- `rating` - рейтинг
- `total` - общая сумма
- `status` - статус

Удачи! 🚀
