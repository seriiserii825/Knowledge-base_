Math.round(2.4); // 2
Math.ceil(2.1); // 3
Math.round(2.9); // 2
Math.floor(Math.random() * 10 + 1); // 0 - 10

const arr = ['red', 'black', 'blue', 'pink', 'magento', 'white'];
const value = Math.floor(Math.random() * arr.length); // 0 - 10
console.log(arr[value]);

