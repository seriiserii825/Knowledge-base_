### 📌 Что это?

Цепочка ответственности — это паттерн, который позволяет передавать запрос по цепочке объектов, пока один из них не обработает его.

Каждый объект в цепочке:

- или обрабатывает запрос,
- или передаёт дальше следующему обработчику.

### 📌 Зачем он нужен?

Когда у вас есть много вариантов обработки одного и того же запроса, и вы хотите:

- убрать гигантские if…else if…else
- сделать гибкую систему проверок / правил / фильтров
- дать возможность легко добавлять, убирать или менять обработчики
- не знать заранее, кто именно обработает запрос

### 📌 Когда использовать?

Используй CoR, когда:
✔ Нужно выполнить несколько проверок подряд
✔ Проверки не зависят друг от друга
✔ Проверки можно добавлять / убирать без переписывания кода
✔ Необходимо выбрать один обработчик из многих
✔ Есть сложная логика валидации / правил / middleware

Очень распространённые примеры:
Middleware в Express.js / Nest.js

- Pipeline в Django / Laravel
- Валидации форм
- Последовательная обработка данных
- Авторизация → аутентификация → роли → ACL
- Фильтры: логирование → кеш → throttle → API ключи

### 📌 Как работает (кратко)

Есть Handler — базовый класс/интерфейс

Каждый конкретный обработчик:

делает свою работу
если не его дело → передаёт дальше (next.handle(request))

🧪 Пример на TypeScript (самый понятный)
Базовый интерфейс

```ts
interface Handler {
  setNext(handler: Handler): Handler;
  handle(request: any): any;
}

abstract class BaseHandler implements Handler {
  private nextHandler: Handler | null = null;

  setNext(handler: Handler): Handler {
    this.nextHandler = handler;
    return handler;
  }

  handle(request: any): any {
    if (this.nextHandler) {
      return this.nextHandler.handle(request);
    }
    return null;
  }
}
```

Никакого изменения старых классов.

💡 Плюс 2 — изоляция логики
Каждое правило живёт в своём классе.

💡 Плюс 3 — чистота кода
Никаких монструозных условий:

📌 Где часто встречается в реальной жизни?
✔ Express middleware:

```ts
app.use(authMiddleware);
app.use(roleMiddleware);
app.use(logMiddleware);
```

✔ Валидации форм
Проверка email

- Проверка длины
- Проверка уникальности
- Проверка роли

✔ Логирование
request → logger → performance → analytics

✔ Фильтры в обработке файлов
resize → compress → watermark → upload

📚 Итог: коротко
Вопрос Ответ

- Что? Цепочка обработчиков
- Когда? Когда много разных проверок/фильтров
- Зачем? Чтобы убрать сложные if-else, сделать логику гибкой
- Как?
