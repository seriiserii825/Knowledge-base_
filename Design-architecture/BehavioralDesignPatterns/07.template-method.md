Template Method (Шаблонный метод) — поведенческий паттерн,
который позволяет определить скелет алгоритма в базовом классе,
а конкретные шаги алгоритма перенести в подклассы, чтобы они могли переопределять только нужные шаги.

Проще:
👉 Базовый класс говорит: «Алгоритм выполняется так: 1 → 2 → 3».
А подклассы решают, как именно выполнять эти шаги.

### 📌 Что такое Template Method?

Есть 2 части:

### 1. Abstract Class (абстрактный класс)

задаёт последовательность шагов (алгоритм)

- некоторые шаги конкретные
- некоторые шаги абстрактные → дочерние классы должны реализовать

### 2. Concrete Subclasses (конкретные реализации)

- реализуют абстрактные шаги
- могут переопределять необязательные шаги

### 🧠 Зачем нужен Template Method?

✔ Когда есть общий алгоритм, но детали отличаются
Например:

- Виды экспорта: PDF, CSV, HTML
- Разные авторизации: OAuth, JWT, Basic
- Виды обработки файлов: изображения, текст, аудио

✔ Чтобы избежать копирования кода
Общий алгоритм находится в одном месте.

✔ Чтобы заставить дочерние классы выполнить необходимые шаги
Например, перед сохранением продукта — всегда валидация.

✔ Чтобы следовать DRY + OCP

### 🧩 Когда использовать Template Method?

- Есть общий алгоритм, меняются только отдельные шаги.
- Нужно централизовать порядок выполнения действий.
- У разных реализаций должен быть одинаковый “скелет”.
- Хочешь запретить изменять сам алгоритм, но позволить переопределять шаги.

### 💻 Пример Template Method (TypeScript)

🎯 Пример: Экспорт данных (PDF, CSV)

### 1. Абстрактный класс с шаблонным методом

```ts
abstract class DataExporter {
    // шаблонный метод
    export() {
        this.fetchData();
        this.formatData();
        this.saveFile();
    }

    // шаги алгоритма (некоторые абстрактные)
    protected abstract fetchData(): void;
    protected abstract formatData(): void;

    // общий шаг
    protected saveFile() {
        console.log("Saving file to disk...");
    }
}
```
### 2. Конкретные реализации

📄 PDF Exporter
```ts
class PdfExporter extends DataExporter {
    protected fetchData() {
        console.log("Fetching data for PDF...");
    }

    protected formatData() {
        console.log("Formatting PDF...");
    }
}
```
### 📊 CSV Exporter

```ts
class CsvExporter extends DataExporter {
    protected fetchData() {
        console.log("Fetching data for CSV...");
    }

    protected formatData() {
        console.log("Formatting CSV...");
    }
}
```
### 3. Использование

```ts
const pdf = new PdfExporter();
pdf.export();

const csv = new CsvExporter();
csv.export();
```
🧁 Аналогия

👉 Процесс приготовления торта:

- Замешать тесто
- Выпечь
- Украсить

Скелет один и тот же, но:

- украшение может быть разным,
- вкус может быть разным,
- тесто может быть разным.

Template Method = общий рецепт + вариации.

### 🆚 Template Method vs Strategy

Template Method	Strategy

- Основа — наследование	Основа — композиция
- Алгоритм фиксированный, меняются шаги	Алгоритм полностью заменяем
- Структура задаётся раз и навсегда	Алгоритм выбирается динамически
- Жёсткая структура	Гибкая структура

### 🆚 Template Method vs Factory Method

Template Method	Factory Method

- Определяет скелет алгоритма	Определяет создание объекта
- Шаги алгоритма — абстрактные	Фабрика создаёт продукты
- Основная идея — контроль алгоритма	Основная идея — создание

🎯 Итог
Паттерн Template Method:

- Задает шаблон (скелет) общего алгоритма.
- Часть шагов фиксированная, часть — реализуют подклассы.
- Избавляет от дублирования логики.
- Следует DRY, OCP.
- Подходит, когда алгоритм одинаковый, но детали — разные.
