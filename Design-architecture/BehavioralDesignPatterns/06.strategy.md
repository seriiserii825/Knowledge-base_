Strategy (Стратегия) — поведенческий паттерн проектирования, который позволяет выбирать алгоритм поведения во время выполнения, помещая разные варианты поведения в отдельные классы.

Проще:
👉 Есть один интерфейс, и несколько алгоритмов. Клиент подставляет нужный.

Это как:

- Выбрать способ оплаты (карта / PayPal / крипта)
- Выбрать способ сортировки
- Выбрать вид скидки
- Выбрать стратегию доставки

### 📌 Что такое Strategy?

У тебя есть:

1. Context (контекст)
Объект, который выполняет алгоритм.

2. Strategy (интерфейс стратегии)
Определяет общий метод (одинаковая сигнатура).

3. Concrete Strategies (конкретные стратегии)
Разные реализации одного алгоритма.

Контекст не знает, какой именно алгоритм используется — просто вызывает метод стратегии.

### 🧠 Зачем нужен Strategy?


✔ Чтобы заменить "if/else" или "switch"
Вместо:

```ts
if (type === "stripe") { ... }
if (type === "paypal") { ... }
if (type === "crypto") { ... }
```
Ты создаёшь три стратегии.

✔ Чтобы легко добавлять новые алгоритмы
Не меняя код контекста → OCP (Open-Closed Principle).

✔ Чтобы выбирать поведение динамически
Например, способ сортировки по клику.

✔ Чтобы изолировать сложные алгоритмы в отдельные классы
### 📅 Когда использовать Strategy?

Много похожих алгоритмов.

- Нужно подменять поведение во время выполнения.
- Хочешь избавиться от больших условий.
- Алгоритмы могут меняться независимо от контекста.
- Нужно тестировать алгоритмы отдельно.

Реальные примеры:

- Способы оплаты
- Способы скидки
- Способы доставки
- Алгоритмы логирования
- Политики кеширования
- Поведение персонажа в игре

### 💻 Простой пример Strategy (TypeScript)

💰 Пример: расчёт цены товара с разными стратегиями скидок
1. Интерфейс стратегии
```ts
interface DiscountStrategy {
    apply(price: number): number;
}
```
2. Конкретные стратегии
```ts
class NoDiscount implements DiscountStrategy {
    apply(price: number) {
        return price;
    }
}

class PercentageDiscount implements DiscountStrategy {
    constructor(private percent: number) {}
    apply(price: number) {
        return price - (price * this.percent) / 100;
    }
}

class FixedDiscount implements DiscountStrategy {
    constructor(private amount: number) {}
    apply(price: number) {
        return price - this.amount;
    }
}
```
3. Контекст
```ts
class PriceCalculator {
    constructor(private strategy: DiscountStrategy) {}

    setStrategy(strategy: DiscountStrategy) {
        this.strategy = strategy;
    }

    calculate(price: number) {
        return this.strategy.apply(price);
    }
}
```
4. Использование
```ts
const calculator = new PriceCalculator(new NoDiscount());

console.log(calculator.calculate(100)); // 100

calculator.setStrategy(new PercentageDiscount(10));
console.log(calculator.calculate(100)); // 90

calculator.setStrategy(new FixedDiscount(15));
console.log(calculator.calculate(100)); // 85
```
🔁 Аналогия
### 👉 Как выбрать маршрут в навигаторе:

- Быстрый
- Короткий
- Без платных дорог
- Навигатор — контекст, алгоритм маршрута — стратегия.

### 🆚 Strategy vs State

Паттерн	Отличие
Strategy	Клиент выбирает алгоритм
State	Сам объект меняет состояние и поведение

🆚 Strategy vs Template Method
Strategy	Template Method
Алгоритм можно менять на лету	Алгоритм фиксированный, меняются шаги
Полиморфизм через композицию	Полиморфизм через наследование
Гибкий	Жёстче

🎯 Итог
Паттерн Strategy:

- Инкапсулирует разные алгоритмы
- Позволяет динамически менять поведение
- Убирает сложные if/switch
- Делает код гибким и расширяемым
- Идеально подходит для вариантов «выбор поведения»
