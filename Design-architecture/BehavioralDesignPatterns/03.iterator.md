🧩 Iterator — Что, Зачем, Когда и Как
✅ Что это?
Iterator (Итератор) — поведенческий паттерн, который позволяет последовательно обходить элементы коллекции,
не раскрывая её внутреннее устройство.

Другими словами:

«Мне всё равно какой там массив, дерево или граф — дай мне способ пройтись по элементам».

🎯 Зачем нужен?

### 1) Единый способ обхода коллекции

Тебе не нужно знать:

- массив это?
- связный список?
- дерево?
- hash-map?

Просто вызываешь .next().

### 2) Скрывает сложную структуру

Если коллекция — дерево или граф, итератор скрывает:

- рекурсию,
- стек обхода,
- порядок обхода (in-order, pre-order и т.п.).

### 3) Позволяет сделать несколько вариантов обхода одной коллекции

Например:

- итератор вперёд,
- назад,
- только активных элементов,
- только тех, что прошли фильтр.

### 4) Позволяет обходить коллекцию без угрозы её повредить

Итератор создаёт снимок состояния, или управляет позициями сам.

⏱ Когда использовать?
✔ когда нужно проходить разные структуры одинаковым способом;
✔ когда нужно инкапсулировать логику обхода;
✔ когда нужен несколько вариантов итерации;
✔ когда коллекция сложная (дерево, граф, матрица, связный список).

Не используется, если:
❌ коллекция очень простая, и обычный for хватает.

### 🛠 Как работает?

Есть 2 сущности:

1) Iterator — знает как перемещаться

```ts
interface Iterator<T> {
    next(): T | null;
    hasNext(): boolean;
}
```
### 2) Aggregate (Коллекция) — создаёт итератор

```ts
interface IterableCollection<T> {
    createIterator(): Iterator<T>;
}
```

### 🔥 Мини-пример (TypeScript)

Коллекция WordsCollection, которую мы будем обходить:

```ts
class WordsIterator implements Iterator<string> {
    private position = 0;

    constructor(private collection: string[]) {}

    next() {
        return this.collection[this.position++];
    }

    hasNext() {
        return this.position < this.collection.length;
    }
}

class WordsCollection {
    private items: string[] = [];

    add(item: string) {
        this.items.push(item);
    }

    createIterator() {
        return new WordsIterator(this.items);
    }
}

const collection = new WordsCollection();
collection.add("Hello");
collection.add("World");
collection.add("!");

const iterator = collection.createIterator();

while (iterator.hasNext()) {
    console.log(iterator.next());
}
```
Вывод:

diff
Copy code
Hello
World
!

### 🧠 Реальные примеры из мира

✔ Array.prototype[Symbol.iterator]()
✔ Map, Set, NodeList в браузере
✔ jQuery .each()
✔ курсоры в БД
✔ потоки файлов fs.createReadStream()
✔ React Fiber tree (внутренние обходы)
