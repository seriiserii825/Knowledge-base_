Observer (Наблюдатель) — поведенческий паттерн проектирования, который позволяет одним объектам (наблюдателям) автоматически получать уведомления от другого объекта (издателя) при изменениях его состояния.

Проще:
👉 Есть объект, который меняется → все подписчики автоматически узнают об этом.

Это как YouTube:

- Канал = Subject (издатель)
- Подписчики = Observers
- Новое видео = уведомление

### 📌 Что такое Observer?

Есть 2 роли:

1. Subject (Издатель / Observable)
   хранит состояние

- позволяет подписаться/отписаться
- уведомляет всех подписчиков при изменении

2. Observer (Наблюдатель)
   получает уведомления

реагирует на изменения

### 🧠 Зачем нужен Observer?

✔ Автоматические обновления
Например, UI обновляется когда меняются данные.

✔ Отвязка логики
Издатель не знает, что делают наблюдатели.
Он просто сообщает: "У меня что-то изменилось".

✔ Реактивность
Это основа многих библиотек: React, Vue, RxJS.

✔ Масштабируемость
Можно иметь 1→много подписчиков.

📅 Когда использовать Observer?
Нужно оповещать много объектов о событии.

- Объекты должны реагировать по-разному.
- Издатель не должен знать, кто его слушает.
- Данные меняются → интерфейс обновляется.

Примеры:

- UI → изменения в модели данных (MVC, MVVM)
- Любой “event system”
- Авторизация (слушатели logout/login)
- Логи, метрики, оповещения
- Реактивные формы

💻 Простой пример Observer на TypeScript
🎤 Пример: Хранилище данных уведомляет компоненты UI

```typescript
// ===== Observer =====
interface Observer {
  update(data: string): void;
}

// ===== Subject =====
class Store {
  private observers: Observer[] = [];
  private state: string = "";

  subscribe(observer: Observer) {
    this.observers.push(observer);
  }

  unsubscribe(observer: Observer) {
    this.observers = this.observers.filter((o) => o !== observer);
  }

  setState(newState: string) {
    this.state = newState;
    this.notify();
  }

  getState() {
    return this.state;
  }

  private notify() {
    this.observers.forEach((o) => o.update(this.state));
  }
}

// ===== Observers (UI components) =====
class HeaderComponent implements Observer {
  update(data: string) {
    console.log("Header updated:", data);
  }
}

class SidebarComponent implements Observer {
  update(data: string) {
    console.log("Sidebar updated:", data);
  }
}

// ===== USAGE =====
const store = new Store();

const header = new HeaderComponent();
const sidebar = new SidebarComponent();

store.subscribe(header);
store.subscribe(sidebar);

store.setState("User logged in");
store.setState("User updated profile");
```

📌 Оба компонента получают уведомления автоматически.

### 🔁 Аналогия с реальной жизнью

- Погода изменилась → метеостанция оповещает все табло
- Вы подписались на блог → вам приходят новые статьи
- Сервер шлёт вебсокеты → клиенты реагируют

🆚 Observer vs Mediator vs Pub/Sub
Паттерн Суть

- Observer Один объект напрямую уведомляет подписчиков
- Mediator Все общаются только через посредника
- Pub/Sub Как Observer, но через общий EventBus и без прямой ссылки

### 🎯 Итог

Паттерн Observer:

- Уведомляет множество объектов об изменениях
- Полностью разделяет издателя и подписчиков
- Основной механизм реактивного программирования
- Легко добавлять/удалять подписчиков
