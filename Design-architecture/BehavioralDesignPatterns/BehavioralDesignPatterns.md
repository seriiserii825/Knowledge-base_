Behavioral Design Patterns — это поведенческие шаблоны проектирования (из GoF), которые описывают как объекты взаимодействуют, как распределяются обязанности и как организовать логику общения между частями системы.

Если простыми словами:

Поведенческие паттерны — это про то, как объекты себя ведут и как они общаются друг с другом.

🧠 Зачем нужны поведенческие паттерны?
Чтобы:

- убрать жёсткие связи между объектами;
- сделать код расширяемым, без if/else миллион раз;
- разделить ответственность между объектами;
- менять алгоритмы «на лету»;
- централизовать или делегировать принятие решений;
- настроить общение объектов без сильной связанности.

### 📦 Какие паттерны относятся к Behavioral?

Классические 11 GoF поведенческих паттернов:

- Strategy — подменяемые алгоритмы.
- Observer — подписчики на события.
- Command — команда как объект.
- Iterator — последовательный доступ к коллекциям.
- Mediator — центральный объект-координатор.
- Chain of Responsibility — цепочка обработчиков.
- State — состояния объекта вместо условных конструкций.
- Template Method — скелет алгоритма + переопределяемые шаги.
- Visitor — добавление новых операций над объектами.
- Memento — снимок состояния (undo/redo).
- Interpreter — интерпретатор маленького DSL.

### 🧩 Когда использовать поведенческие паттерны?

Когда:

- нужно заменить сложные if-else конструкций → Strategy, State, Chain
- нужно разные реакции разных объектов на событие → Observer
- нужно разделить отправителя и исполнителя действий → Command
- нужно координировать взаимодействие объектов → Mediator
- нужно контролировать историю/откат изменений → Memento
- нужно навешивать новые операции на разные типы объектов → Visitor
- нужно удобно обходить сложные коллекции → Iterator
- нужно дать базовый алгоритм, но менять его шаги → Template Method
