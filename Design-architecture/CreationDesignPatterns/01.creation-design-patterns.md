### 🟦 Что такое creational design patterns?

Порождающие паттерны — это шаблоны, которые помогают
управлять созданием объектов.

Они отвечают на вопросы:

- Как создать объект правильно?
- Как скрыть сложность создания?
- Как сделать создание гибким?
- Как избежать жёсткой привязки к конкретному классу?

### 🟣 Коротко и ясно

Паттерн Для чего

- **Factory Method** - Скрыть создание, выбирать реализацию
- **Abstract Factory** - Создавать семейства связанных объектов
- **Builder** - Пошаговое создание сложного объекта
- **Singleton** - Один объект на всё приложение
- **Prototype** - Создание через копирование

### 🟢 Для чего они нужны (простыми словами)

- Спрятать сложность создания объектов
- Изолировать код от конкретных реализаций (new)
- Работать через интерфейсы, а не конкретные классы
- Облегчить тестирование
- Сделать систему расширяемой

Иными словами:

Creational patterns дают гибкость и контроль над созданием объектов.

### 🟨 Когда они применяются

- если объект создаётся сложно (много параметров, проверки, зависимостей)
- если нужно выбирать одну из реализаций (MySQL/Postgres, Web/Console)
- если объект должен создаваться шаг за шагом
- если нужно скрыть детали создания от пользователя
- если нужно легко подменить реализацию

### 🟩 Простые примеры (короче некуда)

1. Factory Method
   Скрывает new внутри метода.

```ts
interface Transport {
  drive(): void;
}

class Car implements Transport {
  drive() {
    console.log("Car driving");
  }
}

class TransportFactory {
  static create(type: "car") {
    if (type === "car") return new Car();
  }
}

const t = TransportFactory.create("car");
t.drive();
```

2. Abstract Factory

Создаёт семейства объектов, которые должны работать вместе.

```ts
interface Button {
  render(): void;
}
interface Input {
  render(): void;
}

class DarkButton implements Button {
  render() {
    console.log("Dark Button");
  }
}
class DarkInput implements Input {
  render() {
    console.log("Dark Input");
  }
}

class DarkUIFactory {
  createButton(): Button {
    return new DarkButton();
  }
  createInput(): Input {
    return new DarkInput();
  }
}
```

3. Builder

Создаёт сложный объект пошагово.

```ts
class RequestBuilder {
  private url = "";
  private method = "GET";

  setUrl(u: string) {
    this.url = u;
    return this;
  }
  setMethod(m: string) {
    this.method = m;
    return this;
  }

  build() {
    return { url: this.url, method: this.method };
  }
}

const req = new RequestBuilder().setUrl("/users").setMethod("POST").build();
```

4. Singleton

Гарантирует, что объект существует только один раз.

```ts
class Config {
  private static instance: Config;
  private constructor() {}

  static getInstance() {
    return (this.instance ??= new Config());
  }
}

const c1 = Config.getInstance();
const c2 = Config.getInstance();
```

5. Prototype

Создание новых объектов через клонирование.

```ts
const original = { name: "Alice", age: 20 };
const clone = { ...original };
```
