### ⭐ Prototype Pattern — простыми словами

Prototype (Прототип) — это паттерн, который позволяет создавать новые объекты через копирование существующего объекта, а не через new Class().

То есть:

- 👉 Есть один "эталон" — прототип
- 👉 Мы делаем его clone(),
- 👉 Получаем новый объект, но с той же структурой и данными.

Когда полезно:

- создание новых объектов дорогó (много вложенных структур)
- нужно создавать много похожих объектов
- нужно быстро копировать состояние
- конфигурации, пресеты, шаблоны, настройки
- игровые объекты, товары, UI-компоненты, блоки ACF, анимации GSAP

### 🧩 Базовый пример на TypeScript

```ts
interface Prototype<T> {
  clone(): T;
}

class User implements Prototype<User> {
  constructor(
    public name: string,
    public settings: { theme: string; beta: boolean },
  ) {}

  clone(): User {
    return new User(
      this.name,
      { ...this.settings }, // глубокая копия вложенного
    );
  }
}

const user1 = new User("Ivan", { theme: "dark", beta: true });
const user2 = user1.clone();

user2.name = "Alex";
user2.settings.theme = "light";

console.log(user1); // НЕ изменился
console.log(user2);
```
