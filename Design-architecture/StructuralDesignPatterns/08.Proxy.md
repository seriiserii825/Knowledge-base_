❓ Что это
Proxy (Заместитель) — это объект-посредник, который контролирует доступ к другому объекту.

Он ведёт себя как настоящий объект, но добавляет дополнительную логику до или после вызова.

🎯 Зачем нужен Proxy
Когда тебе нужно вставить дополнительный уровень логики, НЕ изменяя основной класс.

Частые причины:

✔ 1. Контроль доступа
Проверить права пользователя перед выполнением операции.

✔ 2. Ленивая загрузка (Lazy Loading)
Отложить создание тяжелого объекта до реального использования.

✔ 3. Локальный кэш
Сохранять результат, чтобы не делать повторные дорогие операции.

✔ 4. Логирование
Записывать в лог каждый вызов, но не модифицируя основной объект.

✔ 5. Сетевые запросы / удалённые сервисы
Создать локальный "представитель" удалённого объекта (Remote Proxy).

🕓 Когда использовать Proxy
Используй Proxy, когда:

- объект тяжёлый (большие данные, сложные вычисления)
- нужно скрыть реальный объект
- нужно добавить безопасность
- нужно добавить кэш
- нельзя или «некрасиво» менять оригинальный класс
- оригинальный объект может находиться на другом сервере

### 🧩 Минимальный пример (TypeScript)

🔍 Пример: Ленивая загрузка изображения

```ts
interface Image {
  display(): void;
}

class RealImage implements Image {
  constructor(private filename: string) {
    console.log("Loading large image:", filename);
  }

  display() {
    console.log("Displaying:", this.filename);
  }
}

class ImageProxy implements Image {
  private realImage: RealImage | null = null;

  constructor(private filename: string) {}

  display() {
    if (!this.realImage) {
      this.realImage = new RealImage(this.filename); // ленивое создание
    }
    this.realImage.display();
  }
}

// --- client ---
const img = new ImageProxy("photo.jpg");
img.display(); // загружает и показывает
img.display(); // показывает, но не загружает снова
```
