### 🎯 Что такое Facade (Фасад)?

Facade — это структурный паттерн, который предоставляет простой унифицированный интерфейс поверх сложной системы.

Он НЕ заменяет систему.
Он создаёт одну простую точку входа, чтобы скрыть сложность под капотом.

### 🧠 Зачем нужен Фасад

Чтобы:

✔ Упростить сложный код
Система состоит из 10 модулей → ты создаёшь один класс Facade → клиент работает только с ним.

✔ Скрыть детали реализации
Внедрили новый сервис или переписали логику — клиентский код не изменяется.

✔ Уменьшить связность (coupling)
Вместо того чтобы вызывать 7 функций в разных местах → вызываешь одну.

✔ Создать “API” внутри кода
Фасад — это как внутренний мини-API для модуля.

### 📌 Когда использовать Facade

Используй Фасад, когда:

• Много шагов для одного действия
Например: заказать товар → рассчитать доставку → проверить оплату → отправить письмо → записать лог.

• Хочешь спрятать «грязный» legacy-код
Фасад даёт новый чистый интерфейс поверх старого.

• Хочешь уменьшить количество зависимостей
Клиент знает только Facade, а не 10 классов.

• Нужно создать удобный модуль для фронта/бэка
Фасад — часто используется как “Service”.

### 🧩 Как работает Facade

Схема:

Клиент → Facade → (Subsystem A)
→ (Subsystem B)
→ (Subsystem C)

Клиент видит только Facade.

🧪 Простой пример на TypeScript (реальный проект)
Допустим, у тебя есть сложный процесс: оформление заказа.

Без фасада:

```ts
const user = userService.getUser(id);
const products = cartService.getProducts();
paymentService.charge(user, amount);
emailService.sendEmail(user.email);
logService.save("Order completed");
```

Код разрастается, повторяется.

### ✔ Решение: создаём Facade

```ts
class OrderFacade {
  constructor(
    private userService: UserService,
    private cartService: CartService,
    private paymentService: PaymentService,
    private emailService: EmailService,
    private logService: LogService,
  ) {}

  async makeOrder(userId: number) {
    const user = this.userService.getUser(userId);
    const cart = this.cartService.getProducts();
    const amount = cart.reduce((sum, p) => sum + p.price, 0);

    await this.paymentService.charge(user, amount);
    await this.emailService.sendEmail(user.email, "Order completed!");
    this.logService.save(`Order for ${user.name} completed`);
  }
}
```

Использование:

```ts
const orderFacade = new OrderFacade(
  new UserService(),
  new CartService(),
  new PaymentService(),
  new EmailService(),
  new LogService(),
);

orderFacade.makeOrder(1);
```

### 🔥 Что мы получили

👍 Клиентский код супер простой

```ts
orderFacade.makeOrder(1);
```

👍 Легко менять внутреннюю логику
Сменил emailService → клиент ничего не замечает.

👍 Легче тестировать
Тестируем только методы фасада.

👍 Декоративное применение
OrderFacade стал API внутри приложения.
