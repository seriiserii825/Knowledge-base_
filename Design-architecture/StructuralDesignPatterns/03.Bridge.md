### 🧱 Что такое Bridge (Мост)?

Bridge — это структурный паттерн, который разделяет один большой класс на две независимые иерархии, чтобы они могли развиваться независимо друг от друга.

То есть:
разделяем “что делаем” и “как делаем”.

🤯 Проблема, которую Bridge решает
Представим, что у нас есть классы:

- Circle
- Rectangle
- Triangle

И нам нужно рисовать их:

- На Canvas
- В SVG
- В PNG

Без паттерна у нас получится:

- CircleCanvas
- CircleSVG
- CirclePNG
- RectangleCanvas
- RectangleSVG
- RectanglePNG
- TriangleCanvas
- TriangleSVG
- TrianglePNG

❌ Иерархия разрастается: 3 фигуры × 3 способа рисования = 9 классов.
❌ При добавлении новой фигуры или нового рендера — всё ломается.

### 🧩 Что делает Bridge?

Он разделяет систему на два слоя:

1) Абстракция — что мы делаем
Например:

- Circle
- Rectangle

2) Реализация — как мы это делаем
Например:

- CanvasRenderer
- SvgRenderer
- PngRenderer

И связывает их "мостом":

Circle --- рендериться через ---> Renderer

Теперь у нас комбинации получаются без удвоения классов.

✔ Пример кода на TypeScript
Реализация (Implementor)
```ts
    interface Renderer {
        drawCircle(x: number, y: number, radius: number): void;
    }

class CanvasRenderer implements Renderer {
    drawCircle(x: number, y: number, radius: number) {
        console.log(`Canvas: circle at (${x},${y}) radius=${radius}`);
    }
}

class SvgRenderer implements Renderer {
    drawCircle(x: number, y: number, radius: number) {
        console.log(`<circle cx="${x}" cy="${y}" r="${radius}" />`);
    }
}
```
### Абстракция (Abstraction)

```ts
abstract class Shape {
    constructor(protected renderer: Renderer) {}
    abstract draw(): void;
}
```
Конкретная абстракция
```ts
class Circle extends Shape {
    constructor(
            renderer: Renderer,
            private x: number,
            private y: number,
            private radius: number
            ) {
        super(renderer);
    }

    draw() {
        this.renderer.drawCircle(this.x, this.y, this.radius);
    }
}
```
Использование
```ts
const canvas = new CanvasRenderer();
const svg = new SvgRenderer();

const circle1 = new Circle(canvas, 10, 10, 50);
const circle2 = new Circle(svg, 100, 100, 30);

circle1.draw();
circle2.draw();
```

🟢 Можно бесконечно комбинировать
🟢 Добавили новый renderer → не трогаем классы фигур
🟢 Добавили новую фигуру → не трогаем renderers

🧠 Когда использовать Bridge?
Используйте Bridge, когда:

✔ 1) Есть две оси изменений
Например:

Ось 1 (Абстракции)	Ось 2 (Реализации)
- Фигуры	Способ рисования
- Уведомления	Каналы отправки
- Платёжные операции	Платёжные провайдеры
- Контроллеры	Хранилища / Базы

✔ 2) Класс разрастается, потому что "делает все сразу"
Например:

```ts
    NotificationService
    - sendEmail()
    - sendSMS()
- sendPush()
```
Bridge позволяет вынести методы отправки в другое семейство классов.

### ✔ 3) Нужно подключать новые реализации, не ломая старый код

Идеально подходит для:

- мультирендерных систем
- драйверов
- платёжных шлюзов
- UI-библиотек
- логгеров (file / console / http / db)

### 🧨 Где реально используется Bridge?

✔ GUI библиотеки
Qt, GTK, Java Swing:
виджет разделён на «абстракцию» и «реализацию ОС».

✔ Библиотеки логирования
Logger — абстракция
Appender — реализация (file, console, http)

✔ ORM
Модель — абстракция
Database driver — реализация

🧩 Мини-пример ближе к реальным проектам
Уведомления + каналы (как Email/SMS адаптер)
Реализация

```ts
interface Channel {
    send(message: string): void;
}

class EmailChannel implements Channel {
    send(message: string) {
        console.log("EMAIL:", message);
    }
}

class SmsChannel implements Channel {
    send(message: string) {
        console.log("SMS:", message);
    }
}
```
### Абстракция

```ts
abstract class Notification {
    constructor(protected channel: Channel) {}
    abstract notify(text: string): void;
}
```
### Конкретные уведомления

```ts
class WarningNotification extends Notification {
    notify(text: string) {
        this.channel.send("⚠ WARNING: " + text);
    }
}

class SuccessNotification extends Notification {
    notify(text: string) {
        this.channel.send("✔ SUCCESS: " + text);
    }
}
```
### Использование

```ts
const email = new EmailChannel();
const sms = new SmsChannel();

new WarningNotification(email).notify("Low disk");
new SuccessNotification(sms).notify("Payment received");
```
🔥 Добавишь PushChannel — НЕ трогаешь классы Notification
🔥 Добавишь ErrorNotification — НЕ трогаешь классы каналов

### 🏁 Итог (если коротко)

Bridge нужен там, где два измерения изменений.
Он разделяет систему на две независимые иерархии:

Абстракции (что делаем)

Реализации (как делаем)

→ значительно уменьшает количество классов
→ даёт гибкую композицию
→ упрощает расширение системы
