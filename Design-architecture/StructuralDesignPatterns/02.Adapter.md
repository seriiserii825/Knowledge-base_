### 🔌 Adapter (Адаптер)

👉 Паттерн, который соединяет два несовместимых интерфейса.

Если два класса не могут работать вместе, потому что у них разные методы, разные форматы данных или другая структура, — адаптер делает их совместимыми.

### 🧠 Суть паттерна в 1 предложении

Adapter — это «переходник», который преобразует интерфейс одного объекта в тот, который ожидает другой объект.

### 🧱 Зачем нужен адаптер?

1. У тебя есть чужой/старый код
   Который НЕ МОЖНО менять.

Например:

- сторонняя библиотека
- legacy код
- API, на которое нет влияния
- старая структура данных

А твой код требует другой формат.

### 2. У тебя уже есть интерфейсы, которые должны работать вместе

Но по типу:

Client ожидает A
Но у меня есть только B

### 3. Ты меняешь реализацию без изменения клиентского кода

Например, смена:

- системы логирования,
- системы хранения,
- платежного сервиса,
- сервиса отправки уведомлений,

API провайдера.

### 🧱 Когда использовать Adapter?

Используй, когда:

✔ У клиента ожидаемый интерфейс отличается от фактического
Например, клиент вызывает send(message),
а библиотека умеет только push(data).

✔ Хочешь сделать новую систему совместимой со старой
Пример:
Ты обновил API, но весь старый код ожидает старый формат.

✔ Подключаешь внешние библиотеки
У библиотек обычно свой интерфейс, а тебе нужен единый формат.

✔ Нужно приводить разные структуры данных к единому виду
Например, разные API возвращают:

```ts
API1: {name: "John"}
API2: {full_name: "John Doe"}
API3: {firstName: "John", lastName: "Doe"}
```

А ты хочешь иметь один интерфейс User.

### 🧰 Пример в реальной жизни

Переходник для розетки.

- У тебя розетка EU.
- У зарядки USA штекер.

Саму зарядку ты менять не можешь — она закрыта.
Но ты хочешь зарядить телефон.

Ты покупаешь адаптер.

### 🧩 UML (очень упрощённо)

````ts
Client → Target Interface → Adapter → Adaptee
Client — ожидает метод request()
Target — интерфейс, который нужен клиенту
Adaptee — реальный объект, но у него метод specificRequest()
Adapter — превращает request() → specificRequest()
    ```

### 🧪 Простой пример на TypeScript

🚫 Без адаптера — несовместимые интерфейсы

```ts
class OldPaymentSystem {
    makePayment(sum: number) {
        console.log("Old system paid:", sum);
    }
}

interface NewPayment {
    pay(amount: number): void;
}

function processPayment(system: NewPayment) {
    system.pay(100);
}

const old = new OldPaymentSystem();
processPayment(old); // ❌ Ошибка: нет метода pay()
````

### ✅ С адаптером — совместимы

```ts
class OldPaymentSystem {
  makePayment(sum: number) {
    console.log("Old system paid:", sum);
  }
}

interface NewPayment {
  pay(amount: number): void;
}

// Адаптер
class PaymentAdapter implements NewPayment {
  constructor(private oldSystem: OldPaymentSystem) {}

  pay(amount: number) {
    this.oldSystem.makePayment(amount);
  }
}

// Клиентский код
function processPayment(system: NewPayment) {
  system.pay(100);
}

const old = new OldPaymentSystem();
const adapter = new PaymentAdapter(old);

processPayment(adapter); // ✔ Работает!
```

### 🔥 Пример из реальных проектов

📌 Пример: логирование
Ты хочешь единый интерфейс:

```ts
interface Logger {
  log(message: string): void;
}
```

Но ты подключил 3 библиотеки:

- pino
- winston
- bunyan

Каждая имеет разный интерфейс.

Ты пишешь по адаптеру к каждой:

```ts
class PinoAdapter implements Logger { ... }
class WinstonAdapter implements Logger { ... }
class BunyanAdapter implements Logger { ... }
```

И теперь весь проект использует единый интерфейс Logger, а менять библиотеку можно в 1 минуту.

### 🔥 Пример: адаптация API

Три API возвращают пользователей в разных форматах:

```ts
// API A
{ name: "John" }

// API B
{ full_name: "John Doe" }

// API C
{ firstName: "John", lastName: "Doe" }
```

Ты делаешь адаптер, который приводит всё к:

```ts
interface User {
  fullName: string;
}
```

Таким образом клиентский код вообще не знает про структуру API.

### 🔥 Пример: Vue / React приложения

В компонент приходит объект:

```ts
props: {
  product: Object;
}
```

Но разные API могут отдавать:

price vs cost
picture vs image.url vs img_path
Ты делаешь адаптер, который приводит это в единый формат.
