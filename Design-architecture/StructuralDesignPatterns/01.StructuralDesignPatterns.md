Structural Design Patterns (Структурные шаблоны проектирования) — это группа паттернов из GoF, которые помогают строить более гибкую, модульную и расширяемую архитектуру, комбинируя классы и объекты в новые структуры.

Если коротко:
👉 они отвечают за то, как связать объекты между собой, чтобы система была стабильной, гибкой и легко изменяемой.

### ⭐ Зачем нужны структурные паттерны?

Потому что код со временем разрастается: появляются зависимости, копипаст, “костыли”.
Структурные паттерны позволяют:

✔ Уменьшать связанность (loose coupling)
Классы не знают друг о друге лишнего → меньше багается, легче тестировать.

✔ Упрощать работу со сложными структурами
Например, дерево объектов, много адаптеров, сложный API.

✔ Инкапсулировать детали реализации
Внешний код работает через фасад, декораторы, адаптеры — и не знает деталей.

✔ Расширять поведение без переписывания классов
Например, через Декоратор или Компоновщик.

### ⭐ Когда использовать структурные паттерны?

Используй их, когда чувствуешь в проекте:

«Слишком сложно передавать зависимости»

- «Класс ожидает один интерфейс, а у меня другой»
- «Хочу добавить функционал, но не трогать старый код»
- «У меня много вложенных объектов / файлов / UI-деревьев»
- «Я хочу скрыть внутреннюю сложность подсистемы»
- «Хочу управлять доступом к объекту»

### ⭐ Какие паттерны относятся к Structural?

Вот 7 структурных шаблонов:

Шаблон Для чего нужен

- Adapter Преобразует один интерфейс в другой, чтобы объекты могли работать вместе.
- Bridge Разделяет абстракцию и реализацию, чтобы менять их независимо.
- Composite Работа с деревьями объектов (например, меню, категори дерево).
- Decorator Добавляет новое поведение объекту без изменения кода класса.
- Facade Упрощает сложную систему, предоставляя простой API.
- Flyweight Экономит память, разделяя общие данные между объектами.
- Proxy Контролирует доступ к объекту (кэширование, lazy loading, логирование).

### ⭐ Примеры в реальной жизни

1. Adapter → переходник розетки
   У тебя розетка европейская, штекер американский → нужен адаптер.
   В коде: приводим несовместимые интерфейсы к одному виду.

2. Decorator → одеть одежду на человека
   У тебя человек: base object.
   Одеваешь +шапка, +куртка, +шарф — получаются "слои" поведения.

3. Composite → папки и файлы
   Папка может содержать файлы или другие папки.
   Метод getSize() работает одинаково.

4. Facade → «одна кнопка — и всё работает»
   Включить кофемашину → внутри куча подсистем, но пользователь не знает.

5. Proxy → охранник у двери
   Охранник проверяет, можно ли пустить человека внутрь.
   В коде: check permissions, caching, lazy loading, retry, logging.

### ⭐ Самое простое объяснение

Если Creational patterns отвечают на вопрос:
➡ «Как создавать объекты?»

То Structural patterns отвечают на вопрос:
➡ «Как организовать, связать и расширить объекты, которые уже есть?»
