Decorator — это структурный паттерн, который позволяет добавлять объекту новые обязанности “вокруг” него, не изменяя его класс и не ломая существующий код.

Думай так:
есть базовый объект → ты “оборачиваешь” его в оболочку (декоратор) → оболочка делает что-то ДО/ПОСЛЕ вызова оригинального метода, но снаружи всё выглядит как тот же самый объект.

### 1. Зачем нужен Decorator

Проблема без паттерна:

Есть класс OrderService с методом createOrder().

Хочется:

- добавить логирование,
- добавить валидацию,
- добавить кэш,
- добавить метрики…

### Самое очевидное:

Написать кучу наследников: LoggedOrderService, CachedOrderService, ValidatedOrderService, LoggedCachedOrderService, … — комбинаторный ад.

Или засунуть всё в один класс → нарушение SRP и OCP.

Decorator решает это так:

- Есть один интерфейс, например OrderService.
- Есть базовая реализация BaseOrderService.

Есть декораторы:

- LoggingOrderServiceDecorator
- ValidationOrderServiceDecorator
- CachingOrderServiceDecorator

Ты можешь комбинировать их в рантайме:

```ts
const service = new LoggingOrderServiceDecorator(
  new CachingOrderServiceDecorator(new BaseOrderService()),
);
```

### 2. Структура паттерна

Классическая схема:

- Component — общий интерфейс / абстрактный класс (что умеет объект).
- ConcreteComponent — реальный объект (основная логика).
- Decorator — базовый абстрактный декоратор:
- реализует тот же интерфейс Component,
- хранит ссылку на другой Component и делегирует ему вызовы.
- ConcreteDecoratorA/B — конкретные “обёртки”, которые:
- делают что-то ДО/ПОСЛЕ делегирования,
- добавляют новые фичи, не меняя исходный класс.

### 3. Простой пример на TypeScript

Представим, что есть интерфейс уведомлений:

```ts
interface Notifier {
  send(message: string): void;
}

// Базовый компонент
class EmailNotifier implements Notifier {
  send(message: string): void {
    console.log(`📧 Email sent: ${message}`);
  }
}
```

Теперь добавим декоратор:

```ts
// Базовый декоратор
abstract class NotifierDecorator implements Notifier {
  constructor(protected wrappee: Notifier) {}

  send(message: string): void {
    this.wrappee.send(message); // дефолт — просто делегируем
  }
}
```

Конкретные декораторы:

```ts
// Добавляем SMS-уведомление
class SmsNotifier extends NotifierDecorator {
  send(message: string): void {
    super.send(message); // сначала обычное поведение
    console.log(`📱 SMS sent: ${message}`);
  }
}

// Добавляем push-уведомление
class PushNotifier extends NotifierDecorator {
  send(message: string): void {
    super.send(message); // сначала предыдущие декораторы / базовый
    console.log(`📲 Push sent: ${message}`);
  }
}
```

Использование:

```ts
function decoratorExample() {
    // Базовый отправитель — только Email
    const emailNotifier = new EmailNotifier();

    // Оборачиваем его в SMS
    const emailSmsNotifier = new SmsNotifier(emailNotifier);

    // Оборачиваем ещё в Push
    const fullNotifier = new PushNotifier(emailSmsNotifier);

    fullNotifier.send("Order #123 created");
    // Логика:
    // 1) Email sent...
    // 2) SMS sent...
    // 3) Push sent...
}
```
Ключевые моменты:

- Весь код работает через один интерфейс Notifier.
- Мы можем свободно комбинировать декораторы.
- Базовый класс EmailNotifier не меняется.

4. Когда использовать Decorator
   Использовать, когда:

- Нужно добавлять поведение “поверх” уже существующего объекта, не трогая его код.
- Логирование.
- Кэширование.
- Валидация.
- Метрики / трейсинг.
- Ретраи запросов.
- Есть много комбинаций фич, и наследование даёт взрыв классов:
- Logged + Cached + Validated + Traced → без декоратора получишь тонну классов.
- Класс уже в библиотеке (чужой код), менять его нельзя, а поведение расширить нужно.
- Нужно включать/выключать фичи динамически:
- в dev окружении — логирование;
- в prod — кэш + метрики.

5. Плюсы и минусы
   Плюсы:

- Соблюдение OCP — базовый класс закрыт для модификаций.
- Гибкие комбинации поведения без десятка наследников.
- Можно добавлять фичи в рантайме.
- Локализуешь кросс-секционные вещи (лог, кэш, auth и т.д.) в отдельные классы.

Минусы:

- Много мелких классов → сложнее читать структуру.
- Отладка чуть сложнее: цепочка обёрток.
- Иногда сложно понять, в каком порядке применяются декораторы.

